#!/usr/bin/env perl
use strict;
use warnings;

# assign variable
my $string = 'Hello World!';
# display value
print "$string\n";

my $ms = "this is a multiline string
because it contains new line char";

my $cs = "String that has # dir, should not be confused as a comment";
